# Copy to .env and set your Hugging Face API token
HF_API_TOKEN=your_hf_api_token_here
PORT=3000
# Optional: Use Craiyon public API for generation (no token). Set to "true" to enable.
USE_CRAIYON=false
